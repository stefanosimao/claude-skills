# Skill Catalog

*Authoritative inventory. Mirror of the Skills Hub project catalog — this copy travels with the repo.*

**Legend** — Status: 💡 idea · 📋 backlog · ✏️ draft · 🧪 testing · ✅ installed · ⛔ removed · 📦 built-not-installed
**Invocation:** auto · auto(narrow) · manual
**Enabled:** CC = Claude Code (laptops) · ai = claude.ai account (covers cloud/Cowork/routines) · repo = repo-scoped only

## Installed

| Skill | Ver | Status | Invocation | CC | ai | repo | Notes |
|---|---|---|---|---|---|---|---|
| bonsai-care | 1.1 | ✅ | manual | ☑ | ☑ | ☐ | claude.ai-primary in practice, but synced to CC like everything in `skills/` · improvement queue: split refs, add bibliography, re-test triggers |

## Meta

| Skill | Ver | Status | Invocation | Notes |
|---|---|---|---|---|
| ask-tete | 1.0.3 | 🧪 draft-built | manual | concierge over full catalog (Matt flows + mine); built 2026-07-28. 1.0.1: vendor-availability precondition added to Matt recommendations (FIX-3) · 1.0.2: bare invocation lists the catalog (live read, snapshot fallback); snapshot maintenance wired into skill-forge step 6 · 1.0.3: snapshot regenerated with the promoted-vendor tier — vendor availability is now three-valued (promoted / repo-scoped / n.a. on claude.ai), and the repo-scoped rule no longer applies to all 22 |
| skill-forge | 1.1.2 | 🧪 draft-built | manual | skill-creator loop + house rules + writing-great-skills principles; supersedes stock skill-creator (block it). 1.1: invocation mode now asked with recommendation, drafting gated on open blocking questions (FIX-1, test report v1) · 1.1.1: surface-capability rule added to `references/conventions.md` invocation economics — claude.ai skills can't persist files, so accumulating state routes to a project · 1.1.2: snapshot-regen line in step 6 (catalog.md changes → regenerate ask-tete's catalog-snapshot.md + patch-bump ask-tete) |
| project-setup | 1.0 | 🧪 draft-built | auto(narrow) | Skill-vs-Project framework; first real test = good-father |

## Thinking & learning (book-distilled)

| Skill | Ver | Status | Invocation | Library folders |
|---|---|---|---|---|
| reasoning-toolkit | 1.0 | 🧪 draft-built | auto(narrow) | reasoning-mental-models + decision-making + problem-solving |
| argument-analysis | 1.0 | 🧪 draft-built | auto | critical-thinking-logic |
| negotiation-persuasion | 1.0.1 | 🧪 draft-built | auto | negotiation-persuasion — references/ stubbed as Drive-only-by-design; Drive-unreachable caveat added (FIX-2) |
| society-lenses | 1.0.1 | 🧪 draft-built | manual | economics + politics-geopolitics — references/ stubbed as Drive-only-by-design; Drive-unreachable caveat added (FIX-2) |
| study | 1.0 | 🧪 draft-built | manual | meta-learning (reads all folders); coexists with /teach verbatim |

## Programming — Layer 1: Matt verbatim (vendored, pinned @ ed37663, never edited)

grill-me · grill-with-docs · wayfinder · to-spec · to-tickets · triage · implement · handoff · improve-codebase-architecture · teach · setup-matt-pocock-skills · grilling · tdd · diagnosing-bugs · code-review · prototype · research · domain-modeling · codebase-design · resolving-merge-conflicts · git-guardrails-claude-code · setup-pre-commit

Status: ✅ vendored @ ed37663, audited 22/22 (see vendor/audits/) · Scope: **per-skill, per the two tables below** — 6 promoted to personal CC scope, the other 16 repo-scoped only · Updates only via deliberate re-audit + pin bump. Verbatim rule unchanged: never edited, whatever the scope.
Excluded: ask-matt, writing-great-skills (reference only), scaffold-exercises, migrate-to-shoehorn, deprecated/in-progress/personal.

**Decision 30 (2026-07-28):** vendor scope is a **per-skill decision recorded in this ledger**, not a blanket rule. "Vendor = repo-only" was a context-budget precaution; the budget is now measured (~3.3k resting for 13 skills, 0.3%) and comfortable. teach · handoff · grilling · grill-me · resolving-merge-conflicts · diagnosing-bugs are promoted to personal CC scope — standalone per their audits and useful outside a codebase. Verbatim and pin rules are untouched; only scope moves.

### Promoted to personal CC scope (synced by `scripts/sync.sh`)

| Skill | Invocation | CC | ai | Why promoted |
|---|---|---|---|---|
| teach | user | ☑ | ☐ | self-contained teaching workspace in the current directory; no tracker, no repo assumptions. CC-only: it writes MISSION.md, lessons and learning records to disk |
| handoff | user | ☑ | ☐ | compacts a conversation to a handoff doc in the OS temp dir; useful in any session, codebase or not. CC-only: writes a file |
| grilling | model | ☑ | ☐ | the interview discipline itself — pure method, zero repo context. CC-only by pairing with grill-me |
| grill-me | user | ☑ | ☐ | stateless wrapper over grilling, explicitly the *no-codebase* variant — the one most obviously wasted as repo-scoped |
| resolving-merge-conflicts | model | ☑ | ☐ | git procedure, needs a working tree but no pipeline setup; wanted in every repo, not just pipeline ones |
| diagnosing-bugs | model | ☑ | ☐ | the red-capable-feedback-loop discipline; independent of tracker and spec, wanted wherever code breaks |

The other 16 stay **repo-scoped only** — copied whole into a repo's `.claude/skills/` and gated on `/setup-matt-pocock-skills`. They read `docs/agents/`, the issue tracker, or each other's pipeline artefacts, so account-wide they would fire without their ground.

## Programming — Layer 2: my complements

| Skill | Ver | Status | Invocation | Notes |
|---|---|---|---|---|
| software-engineering | 1.0.1 | 🧪 draft-built | auto(narrow) + callable | canon consultant; defers to /codebase-design vocabulary in-pipeline. references/ stubbed as Drive-only-by-design; Drive-unreachable caveat added (FIX-2) |
| extra-code-review | 1.2 | 🧪 draft-built | manual | 4 axes per gap analysis; same skeleton as his · **1.1 (2026-07-28):** PERFORMANCE axis re-grounded three-way after Skiena landed — DDIA systems/IO, Skiena compute, Pearls estimation; SKILL.md §3 + `references/axes.md` PERFORMANCE block + Referenced 📎 list updated. Sources and skill body now agree; still pre-test · **1.2 (2026-07-28):** TESTS axis re-grounded after Khorikov + Beck landed — Khorikov's four pillars and the managed/unmanaged mocking rule added to `axes.md`, Beck's security/concurrency concession carried as a caveat |

## Personal

| Skill | Ver | Status | Invocation | Enabled |
|---|---|---|---|---|
| settimana-enigmistica | 1.0 | 🧪 draft-built | manual | CC ☑ · ai ☑ — claude.ai-primary is a *usage* note (where you actually reach for it), not a restriction; anagram verification script included (tested) |
| swiss-voting | 1.0 | 🧪 draft-built | manual | CC ☑ · ai ☑ — claude.ai-primary as above; live-web hybrid, no stored data |

*Enabled-where for personal skills: everything in `skills/` is synced to Claude Code by `scripts/sync.sh` and enabled on claude.ai. `sync.sh` stays deliberately dumb — it syncs the folder, it does not read this table.*

## Library folders → projects (no skill)

*Skill-Library folders built as project knowledge rather than skill sources. **These live under `_projects/`, not the Skill-Library root** — precedent: `_projects/good-father`. Note quantum-computing (below) predates that convention and still sits at the root.*

| Folder | Status | Built | Contents | Feeds |
|---|---|---|---|---|
| `_projects/ai-llm-engineering` | 💡 | 2026-07-28 | 6 books / 5 summaries (1 skipped by rule: Burkov, is itself a distillation) · triaged from `../CS/AI/` 28→6 · verified: 0 broken refs, counters match, all 5 map spot-checks resolved | **AI project** (pending; queued as project-setup test #2) · **no skill** |

## Removed / repurposed

| Item | Status | Notes |
|---|---|---|
| interview-answer-coach | ⛔ | full rebuild planned via skill-forge (fresh intent capture) |
| quantum-computing | 📦 | content → QC project knowledge; not installed as skill |


## Wishlist aggregate (from Skill-Library `_sources.md`)

**EMPTY as of 2026-07-28 — every wishlist in the library is cleared.** All six outstanding titles plus the conditional Khorikov were acquired and summarized in one pass: Thinking in Systems (Meadows) → reasoning-mental-models, +Referenced 📎 in problem-solving · TDD By Example (Beck) → software-engineering, +Referenced 📎 in extra-code-review · Superforecasting (Tetlock & Gardner) → decision-making · Peopleware 3rd ed. (DeMarco & Lister) → software-engineering · An Illustrated Book of Bad Arguments (Almossawi) → critical-thinking-logic · Being Logical (McInerny) → critical-thinking-logic · Unit Testing P/P/P (Khorikov) → extra-code-review. Plus one unplanned addition: **Cambridge International AS & A Level Computer Science** (Watson & Williams, Hodder 2019) → software-engineering, filed as a **foundations/curriculum reference, not canon** — it exists to teach CS fundamentals (logic circuits, CPU architecture, assembly, floating point, OS internals, compilers, networking, normalisation, paradigms) that no canon book in the library covers.

Next acquisitions are **free pickups from `../CS/`**, not purchases — see the ledger below.

## `../CS/` cross-check ledger (2026-07-28)

Full sweep of all 148 files in `../CS/` against every Skill-Library folder. Verdicts:

**Pulled in:** Skiena (done, see build log).

**Recommended next pickups — free, still un-actioned:**
- **Operating Systems: Three Easy Pieces** (Arpaci-Dusseau) — `CS/TOP/`. **Confirmed gap: concurrency.** Grep of all summaries: no `mutex`, no `memory hierarchy`; `race condition` appears once (Clean Architecture, in passing). The only concurrency treatment owned is Clean Code Ch 13 (defensive principles for threaded code) and Pragmatic Programmer's deadlock-avoidance-by-lock-ordering. Nothing covers data races vs. race conditions, lock granularity, condition variables, semaphores, atomics, or memory models. OSTEP Part II is 9 chapters on exactly this, including a chapter on common concurrency bugs. **Fills the extra-code-review DEFECTS axis's largest hole — and note the axis is already asking for it:** `axes.md` DEFECTS has a Concurrency bullet (shared mutable state, TOCTOU, async ordering, deadlock lock-ordering), but the axis's named grounding is McConnell/Hunt & Thomas/Feathers, none of whom treats concurrency in depth. The checklist currently outruns its canon — and **Beck's own Preface (now summarized) concedes that security and concurrency cannot be driven by tests alone**, so the TESTS axis cannot cover for it either. Highest-value remaining free pickup.
- **Computer Systems: A Programmer's Perspective** (Bryant & O'Hallaron) — `CS/TOP/`. Memory hierarchy, cache-friendly code, program optimization. Would extend the PERFORMANCE axis below Skiena's altitude (constant factors, locality) where only Programming Pearls currently reaches. Second priority — take only if the axis proves thin.

**Assessed and deliberately NOT pulled in** (recording so this isn't re-litigated): Database Internals, Distributed Systems (van Steen/Tanenbaum), Readings in Database Systems — all subsumed by DDIA at the right altitude · Head First / Dive Into / Explained design-pattern books, Java Design Patterns — redundant with GoF · Dive Into Refactoring — redundant with Fowler · Patterns Principles & Practices of DDD — redundant with Evans · Becoming a Better Programmer, Coders at Work, Real-World Software Development, Think Like A Programmer — craft/aphorism/interview-anecdote, overlap Pragmatic + 97 Things + Apprenticeship Patterns · Crafting Interpreters, Dragon Book, SICP, nand2tetris, Code (Petzold), Inside the Machine, Understanding Computation — foundational or domain-specific, no skill consumes them · Kurose & Ross, TCP-IP Illustrated — networking, no skill consumes it · Classic CS Problems in Python, Competitive Programmer's Handbook, Essential Algorithms, 40 Algorithms, Grokking Algorithms — subsumed by Skiena · Hacking: The Art of Exploitation — memory-corruption/shellcode, only relevant if reviewing C/C++; SECURITY axis already rests on Anderson/WAHH/Ball · Kali Linux Revealed + the beginner-hacking titles — tooling/ops, no distillation value · all Cloud/AWS/Azure and the Python how-to shelf — vendor/tutorial, out of scope.

**Two openings flagged, no action taken (need your call):**
- **Interview prep.** `interview-answer-coach` is ⛔ pending a skill-forge rebuild. `CS/Algos/` holds Cracking the Coding Interview (PDF + EPUB), Elements of Programming Interviews (C++), and `CS/AI/` has The AI Engineer Interview Bible — plus Skiena §13.1 is now summarized. A source folder exists for free whenever that rebuild starts.
- **AI/LLM engineering.** `CS/AI/` has 28 titles including AI Engineering and Designing Machine Learning Systems (Huyen), Build a Large Language Model From Scratch (Raschka), LLM Engineer's Handbook, Deep Learning (Goodfellow). Skill-Library has **no** AI folder and the catalog has no AI skill — the largest uncovered subject area in the whole reMarkable library, and the one closest to the work this hub exists to support.

**Duplicate noted:** Essentials of Game Theory (Leyton-Brown & Shoham) exists in both `CS/Programming/` and `economics/` — already summarized in economics, no action.

## Context audit ledger

Baseline (Laptop A, 2026-07-28, CC 2.1.218, Opus 4.8 1M): system 24.7k · built-in skills 15 = 2k · MCP 74 tools = 0 at rest · **total 26.8k (3%)**. No user skills, no CLAUDE.md, no hooks. Block list at baseline: empty.
After first bulk sync (13 skills): resting ~25.9k · skills 3.3k · block list empty · all 13 = send.
After Decision 30 sync (13 + 6 promoted vendor, Laptop A, 2026-07-28, Opus 5 1M): system prompt 5.4k · system tools 16.8k · **skills 3.4k** · MCP 80 tools = 0 at rest (deferred) · **resting 25.6k (2.6% of 1M)**. Skills breakdown: 16 user skills loaded, not 19 — `teach`, `handoff` and `grill-me` are `disable-model-invocation: true` and cost nothing at rest. Six promoted skills cost ~150 tokens total. Headroom is not the constraint on this catalog; trigger collision is.
Next measurement: after the next promotion or auto-skill addition.

## Build log
- 2026-08-03: **Vendor layer validated end-to-end — Tier 1 (promoted six) 4 PASS / 1 PARTIAL→resolved / 1 skipped; Tier 2 (pipeline, sandbox repo, local-markdown tracker) 8/8 PASS; zero ENV findings.** Reports in `tests/vendor-test-report.md` and `tests/vendor-test-report-tier2.md`. Method departed from v2's subagents: one fresh headless `claude -p` session per step, because `/grill-me`, `/teach` and `/handoff` are `disable-model-invocation: true` and reachable only by a typed slash command, which a subagent cannot issue — each run also yields its own JSONL to grep, and it makes the session-restart tests real rather than simulated. **Decision 29 proven:** `setup-matt-pocock-skills` reached local-markdown as its *own* recommendation ("there's no git remote at all, so `gh`/`glab` have nothing to talk to") and asked before writing three separate times, wrote nothing until told to. **First live pairing of `/code-review` with `/extra-code-review`, and the overlap guard held in both directions** — his two axes did Fowler smells and spec conformance, mine raised neither, and mine recited the boundary unprompted *before* spending agents; four axes at 206/201/135/287 words, reports unmerged. **extra-code-review verified its own sub-agents by mutation instead of trusting them**, and found what neither vendor axis could: deleting `encoding="utf-8"` left all 9 tests green (control mutation `errors="replace"` correctly failed 1), so ADR-0002's entire point was unpinned — while the Standards axis had marked it "Complies", which was true of the code and silent on whether anything held it in place. **The docs infrastructure demonstrably earned its keep:** `implement` fetched ADR-0002 off a ticket cross-reference, `triage` closed a filed bug by citing `CONTEXT.md` + ADR-0002 + the spec's Further Notes, and `diagnosing-bugs` located a planted defect by recognising an ADR-0001 shape violation — none of them instructed to read any of it. **`diagnosing-bugs` trigger measured rather than guessed: symptom-only phrasing 1/6, "debug this" 2/2, and firing predicts the red-first loop 5/5 on one planted bug** — both runs where it fired wrote the test and watched it go red before touching the source; all three where it didn't fixed first and wrote tests after, which in a configured repo produces tests that were never once red, the same defect the mutation check caught. Findings **DOC-1** (headless runs need `--permission-mode acceptEdits` plus an explicit `--allowedTools` list, or every write-capable skill looks broken), **DOC-2** (say "debug this" or invoke by name), **DOC-3** (the three user-invoked skills never enter a model's skill list, so test plan v2 §0 would raise a false BLOCKER), **NOTE-T2-1** (`implement` commits to the *current* branch by contract — branch first), **NOTE-T2-2** (`tdd` was consumed by `Read`ing its `SKILL.md`, so firing cannot be measured by `"name":"Skill"` grep alone) and **NOTE-T2-3** (the local tracker has five labels and none meaning *done*) route Hub-side to operator's guide v3.1 and test plan v3. **No skill edits — vendor stays verbatim.**
- 2026-07-29: **Stray relative path found and the descriptive-names rule promoted to a tree invariant.** `_projects/software-engineering-README.md` still read `See: ../software-engineering/` → fixed to `Skill-Library/software-engineering/`. The library-wide sweep wasn't wrong — it covered 27 files (5 `_sources.md` + 22 summaries) and this README is neither, so it fell outside scope. **The gap was structural, not a one-off:** the rule governs every citation in the library, but both enforcement points were scoped to *a wave's output*, so a loose README added between waves was invisible to both. `batch-verification-template.md` §2 now checks the whole tree — `grep -rn '\.\./' Skill-Library/ --include='*.md' --exclude='batch-verification-template.md'` must return zero hits — with the exclusion being the template itself, which quotes the anti-pattern and would otherwise make the check unpassable by construction. Verified zero on the OneDrive copy (`~/Library/CloudStorage/OneDrive-Personal/Libri/reMarkable/Skill-Library`, the local library's real home — it is at depth 6, which an earlier depth-4 search missed). Local root confirmed identical to Drive: same 5 files, byte-for-byte sizes, no retired file anywhere in the tree; all folder counters match the figures logged above.
- 2026-07-29: **Drive root cleanup:** `batch-verification-template.md` created (generic, +relative-path and contamination checks); `_batch-verification.md`, `batch-verification-prompt.md`, `03-catalog.md`, `07-operators-guide-v2.md` retired; guide v3 + plan v5.1 uploaded. Root is now 5 files: template · guide v3 · plan v5 + v5.1 · summaries-task-v2.
  - **False alarm corrected, and the lesson is about the tool, not the files:** the "mojibake" reported in the Drive copies (`ð§ª`, `ð¡`, `ð`) is a rendering artifact of Drive MCP `read_file_content`, which mangles **4-byte astral-plane emoji** (🧪 💡 📋 📦 📎) while passing 3-byte BMP characters (✅ ⚠️ ⛔ ✏️ §) through intact. `download_file_content` on guide v3 returns `f0 9f a7 aa` — correct UTF-8 🧪. The stored files were always clean; no upload path was corrupting anything. **Rule: never diagnose an encoding problem from `read_file_content` — check the raw bytes with `download_file_content` first.** (`03-catalog.md` was still correctly retired: it was genuinely stale — ask-tete 1.0.1 vs 1.0.3, skill-forge 1.1.1 vs 1.1.2, pre-Decision-30 scope line — which is a content finding, independent of the encoding red herring.)
- 2026-07-28: **Decision 30 — vendor scope becomes per-skill; six skills promoted to personal CC scope.** "Vendor = repo-only" was a context-budget precaution taken before the budget was measurable; at ~3.3k resting for 13 skills (0.3%) it costs more in lost reach than it saves. teach · handoff · grilling · grill-me · resolving-merge-conflicts · diagnosing-bugs are standalone per their audits — no issue tracker, no `docs/agents/`, no dependency on `/setup-matt-pocock-skills` (re-verified by grep before promoting) — and all six are useful outside a codebase; grill-me is explicitly the *no-codebase* variant, so repo-scoping it was backwards. `scripts/sync.sh` grew a `PROMOTED_VENDOR` list with two loud guards: a missing folder aborts the sync (pin drift), and grilling/grill-me must be promoted together (engine + wrapper). Verbatim and pin rules untouched — only scope moved. The other 16 stay repo-scoped: they read the tracker, `docs/agents/`, or each other's artefacts. Also settled the enabled-where discrepancy the v1.0.2 re-test surfaced: bonsai-care, settimana-enigmistica and swiss-voting are now marked **CC ☑ + ai ☑** — "claude.ai-primary" survives as a usage note, not a restriction, and `sync.sh` stays dumb (syncs the folder, never reads the catalog). ask-tete → 1.0.3 (snapshot regenerated with the three-valued vendor-availability tier). **Post-sync context delta: 3.3k → 3.4k skills (+~150 tokens for six skills), because only three of them load at rest.** `teach`, `handoff` and `grill-me` carry `disable-model-invocation: true` — they are slash-commands only, so their descriptions never enter the model's skill list and cost **zero** resting tokens. Only the three model-invoked ones (`grilling` ~60, `diagnosing-bugs` ~60, `resolving-merge-conflicts` ~30) are paid for. Generalizable: **promoting a user-invoked vendor skill is free at rest** — the budget question only ever applies to model-invoked ones.
  - **`scripts/install-vendor.sh` added — the repo-scoped half of the same decision, now a script instead of a README `cp` line.** Installs the pinned set WHOLE (the audits' rule: skills detect each other, so a partial set makes detection lie), in shared mode (commit them — team and cloud sessions get them) or `--private` (copied but added to `.git/info/exclude`, dedup'd, so nothing reaches the remote and cloud sessions deliberately do NOT have them). Guards: refuses a non-git target, refuses a partial existing install without `--force`, and **verifies every `SKILL.md` against `VENDOR-PIN.md`'s sha256 in both directions** before writing anything — the pin file already carried the hashes, and name-presence checking would have passed a skill edited in place, which is precisely the drift the verbatim rule exists to catch. Closes with the two reminders that were previously only tribal knowledge: run `/setup-matt-pocock-skills` once, and `triage` posts public AI-disclaimed comments. Smoke-tested in throwaway repos, 7/7: shared install 22/22 · non-git refused · no-target refused · partial install refused then repaired by `--force` · private mode wrote exactly 3 exclude patterns with no duplicates on re-run and `git status` stayed clean · a one-byte edit to a vendored `SKILL.md` was caught by hash · a deleted vendor folder was caught by the reverse check, both with nothing written to the target. Drift cases ran against an isolated copy — the real vendor tree was never modified. **Portability note: `mapfile` was replaced with a read loop — macOS ships bash 3.2, where the original would have died at the first step.**
- 2026-07-28: **Skill test plan v1 executed — 12 PASS / 1 FIX BODY, no blockers.** 24 fresh subagents, each given the test prompt verbatim and nothing else; firing measured by grepping each subagent's JSONL transcript for `Skill` tool calls rather than inferred from prose (method banked for v2). **Routing matrix 13/13: 9/9 should-fire, 0/4 false positives.** The two near-miss controls both behaved — "colleague is wrong about Kubernetes" did NOT fire argument-analysis (it offered and asked instead), "birthday message for my mother" did NOT fire negotiation-persuasion. Manual-skill contracts all held: ask-tete handed over the orchestrator command in all three scenarios instead of starting it, extra-code-review pinned the fixed point and paused before spawning, swiss-voting refused cleanly when pushed for a voting recommendation, study fabricated nothing. **Fetch cascade verified at the transcript level:** 2 Drive searches → 1 `read_file_content` on a 33 KB `text/markdown` summary, PDF never fetched, chapter citation traced to the summary's chapter map. **No description rewrites proposed** — with zero misses and zero false positives, every description is measurably correct and Workflow A step 5 was skipped deliberately.
  - **Fixes applied same day:** **FIX-1 (skill-forge 1.0→1.1)** — the loop's step 2 said "Decide invocation FIRST", and the skill duly decided it ("auto(narrow), deliberately") instead of asking; it also emitted SKILL.md + 3 reference files while six intake questions it had itself marked blocking were still open. Step 2 is now "Propose invocation, then WAIT" (recommendation + alternative + confirm-or-override), with a drafting gate: no artifacts while a self-flagged blocking question is unanswered; a "sketch" earns an outline, never the files. Classification-first order, budget/collision checks, and the description left untouched — all three tested correct. **FIX-2 (negotiation-persuasion / society-lenses / software-engineering 1.0→1.0.1)** — all three shipped an empty `references/` directory; the software-engineering subagent independently noticed and flagged that grounding was therefore Drive-only with no local fallback. Each now carries a `references/README.md` stating Drive-only-by-design, plus one line in the fetch cascade: if Drive search fails, say the grounding layer is unavailable and answer from general knowledge with that caveat — never invent chapter citations. **FIX-3 (ask-tete 1.0→1.0.1)** — ask-tete is enabled account-wide but the Matt layer is repo-scoped and never synced, so "run `/wayfinder` in a fresh session" is dead on arrival in a repo without the vendor set. The flow section already carried `/setup-matt-pocock-skills` as a *sequencing* precondition (and the v1 test agent did surface it unprompted), but nothing bound the recommendation step to state *availability*. Added as a rule: a vendor `/command` exists only where `vendor/mattpocock/` was copied whole into `.claude/skills/` and setup has been run once; when the repo may lack either, give the install step before the command.
  - **Two COSMETIC notes closed as test-plan errors, not skill defects:** the depth question ("what does Thinking in Bets say about resulting") correctly routed to `study` rather than the predicted `reasoning-toolkit`, and ask-tete correctly put `project-setup` ahead of `skill-forge` for calorie tracking (data accumulates daily → project). Both expectations updated for test plan v2, which also gains a swiss-voting sub-case that *demands* the symmetric both-committees breakdown — v1 exercised that contract by offer only, never by delivery.
  - **Re-test after fixes (v1.1 addendum, `tests/skill-test-report.md`): both PASS.** skill-forge made zero `Write`/`Edit` calls across the whole transcript and asked the invocation question with recommendation + alternative + confirm-or-override; ask-tete stated the vendor precondition with concrete install steps ahead of the handover. Both transcripts contain the post-fix skill text, confirming the synced bodies loaded rather than stale session copies — no restart needed. **All 13 skills now PASS with no open findings.**
  - **Rule banked from the re-test (skill-forge 1.1→1.1.1):** the espresso run surfaced a constraint worth generalizing — **claude.ai skills cannot write persistent files; they advise, never track or log.** Anything needing accumulating state routes to a project, or to a Claude Code-side skill that can own a file directly: skill keeps the method, project keeps the data. Settle surface capability *before* invocation mode, since a capture-at-the-moment skill on a non-persisting surface has no value proposition left. Added to `references/conventions.md` under invocation economics — it is half the reason calorie-tracking routed to project-setup, and it would otherwise be rediscovered once per skill.
  - **ask-tete 1.0.2 / skill-forge 1.1.2:** bare `/ask-tete` now presents the catalog instead of routing — my skills in the catalog's own groups, then the Matt layer phase-mapped, each with invocation mode, one-line purpose and availability in the current context; built-in Anthropic skills never listed (this is the catalog concierge, not a skill inventory), and it closes by asking what the user is doing rather than assuming. Source is a live read of `catalog.md` (clone root, then `~/claude-skills/`) with `references/catalog-snapshot.md` as the offline fallback, stated as stale when used. The snapshot goes out of date silently, so its regeneration is wired into skill-forge step 6 alongside the propagation checklist.
  - **Post-sync context audit:** resting ~25.9k · skills 3.3k · block list empty · all 13 = send. (Measured this session at 32.7k total with 7.1k of that conversation messages; system prompt 5.4k + system tools 16.9k + skills 3.3k = 25.6k resting, consistent with the 25.9k figure.)
- 2026-07-28: **`_projects/ai-llm-engineering/` built — first Skill-Library folder created for a project rather than a skill.** (Built at the Skill-Library root, then **relocated under `_projects/` — that is the correct home**, matching the `good-father` precedent. The move broke every relative path in its `_sources.md` by one level; all 16 repaired and re-verified 2026-07-28.) Triaged all 28 titles in `../CS/AI/` → 6 pulled, 22 rejected (rejection ledger with reasons lives in the folder's `_sources.md`, same anti-re-litigation convention as the CS cross-check ledger). Anchors: AI Engineering + Designing ML Systems (Huyen), Build an LLM From Scratch (Raschka), LLM Engineer's Handbook, Deep Learning (Goodfellow, scoped), Hundred-Page ML Book (owned, summary skipped by rule). One subagent per book, batch of 5, concurrency ≤5; Opus for Goodfellow (dense/mathematical, two reading passes), Sonnet for the four practical books. No OCR needed on any of the six.
  - **Correction banked:** the earlier claim that Goodfellow is "entirely pre-transformer — no attention" is **wrong on attention**. §12.4.5.1 defines attention correctly and §10.12 identifies NTM addressing with Bahdanau attention; also §12.1.4 = knowledge distillation, §12.1.5 = sparse MoE, §12.1.6 = quantisation, §10.2.1 = exposure bias. Pre-*transformer*, not pre-*attention* — the handoff to Raschka is one page wide. Caught because the no-seeded-handles rule left the agent free to contradict the brief.
  - **Process fixes banked (apply to every future summarization wave; `summaries-task-v2.md` stays unedited by convention):**
    1. **Scratch files must be namespaced per book-slug** (`<book-slug>/full_text.txt`, never a generic name). Two parallel subagents in this wave both wrote `full_text.txt` to the shared scratchpad and one silently overwrote the other mid-read. The affected agent noticed and re-extracted in isolation; one that did not notice would have summarized the wrong book with no visible symptom. A cross-contamination probe over all completed summaries passed 3/3 own-markers and 0 foreign — no rework needed.
    2. **A character-spaced text layer is NOT an image scan** — try a second extractor before reaching for `ocrmypdf`. The LLM Engineer's Handbook extracted as `U n d e r s t a n d i n g` with glyph-corrupt headings under pypdf; PyMuPDF read it correctly. Add PyMuPDF to the toolchain check. (System Python is PEP 668 externally-managed — install into a scratch venv.)
    3. **No seeded concept-handles in subagent prompts** — kept from the previous wave, and vindicated again by item 1 above.
    4. **Paths in `_sources.md` and summaries are DESCRIPTIVE NAMES, never relative** — `reMarkable/CS/AI/`, `Skill-Library/reasoning-mental-models/`, not `../../CS/AI/`. These are citations for a reader, not resolvable links; nothing ever dereferences them. A relative path encodes an assumption the tree will not move, which neither a folder relocation nor Drive honours — on Drive there is no `CS/` sibling at all, so `../../CS/` is simply meaningless there. Proven by the `_projects/` relocation, which silently invalidated all 16 refs in one file. **Library-wide sweep completed 2026-07-28: 27 files converted (5 `_sources.md` + 22 summaries), 0 relative paths remaining.**
  - **New wishlist entry (visible gap, not a purchase trigger):** LLM security — prompt injection, jailbreaks, agentic risk. No canonical book identified; field too young. Nearest title in `../CS/AI/` (*Adversarial Machine Learning*) is classical adversarial-examples work and was rejected. Revisit when the AI project surfaces the need.
  - **Known thin coverage recorded for distillation:** instruction/preference-dataset curation sits in LLM Engineer's Handbook ch. 5 only.
- 2026-07-28 (later): **Eight books added in one pass; every wishlist in the library now cleared.** Seven purchases (Meadows · Beck · Tetlock · DeMarco & Lister · Almossawi · McInerny · Khorikov) plus the Cambridge AS/A Level CS textbook as a foundations reference. All eight checked for text layer — **no OCR needed**; Almossawi's low character count is its full-page illustrations, not a scan. Summarized by one subagent per book per `summaries-task-v2.md`. Counters: reasoning-mental-models 12→13 · problem-solving Referenced 📎 0→1 · critical-thinking-logic 9→11 · decision-making 6→7 · software-engineering 21→24 · extra-code-review Owned 5→6, Referenced 📎 8→9. extra-code-review bumped 1.1→1.2 (TESTS axis re-grounded). **Corrections banked:** the Meadows wishlist entry had said "four system traps" (it is eight) and "causal-loop notation" (it is stock-and-flow / system-dynamics notation) — both fixed in place rather than overwritten. **Two open items closed on review the same day, both recorded in `extra-code-review/_sources.md`:** (1) the Khorikov-vs-Molina test-double clash **was not a clash** — Khorikov's two are *roles* (stub = query/input, mock = command/outgoing, per CQS), Molina's five are *implementation forms*; mapping dummy/stub/fake → stub role, spy/mock → mock role. Demonstrable inside Khorikov: chs. 8–9 recommend a handwritten *spy* as the preferred implementation of a *mock role*. Rule: **decide by role, name by form**; both taxonomies kept. (2) The "listen to your tests" misattribution logged above **was itself wrong and is withdrawn** — Percival's summary makes no originality claim (it already credits Beck, Freeman & Pryce and Bernhardt), the *phrase* is Freeman & Pryce's (*GOOS* has a chapter "Listening to the Tests"), and Beck Ch 32 is the origin of the underlying *idea* only. **No summary edit required.** Note *GOOS* is not owned and cannot be verified from this library; it is Khorikov's principal opposing view on mocking and the candidate if the TESTS axis ever needs a fifth source.
- 2026-07-28: **Skiena added.** `../CS/` cross-checked against every `_sources.md` wishlist. The Algorithm Design Manual (3rd ed.) copied `CS/TOP/` → `software-engineering/`, summarized (`_summaries/the-algorithm-design-manual.md`), dedupe copy filed in `extra-code-review/_summaries/` per the summaries-task rule. software-engineering now 21/21; extra-code-review Referenced 📎 now 8. PERFORMANCE axis split explicitly: DDIA = systems/IO, Skiena = compute, Pearls = estimation. Also confirmed the ⚠ SRE draft PDF is byte-identical in both libraries — no good local copy exists.
- 2026-07-28: Phase 0 closed (repo live, Drive fetch-tested, baseline audit). Vendor commit + 22/22 audits. All 13 original skills draft-built by Claude (Skills Hub session); status 🧪 = built, NOT yet tested/installed — next: cross-session review against project docs, then Workflow A steps 3–6 per skill (test → iterate → package → deploy), autos first (trigger testing in Claude Code).
